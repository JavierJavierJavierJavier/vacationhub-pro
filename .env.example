# Variables de Entorno - VacationHub Pro
# Copia este archivo a .env y completa con tus valores reales

# JWT Secret (generar uno seguro: openssl rand -base64 32)
JWT_SECRET=tu_secreto_jwt_super_seguro_aqui_minimo_32_caracteres

# SMTP Configuration (para emails)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=tu-email@alter-5.com
SMTP_PASS=tu_contraseña_de_aplicacion

# Server Configuration
PORT=4000
NODE_ENV=production

# Frontend URL (para links en emails)
APP_URL=http://localhost:5173
